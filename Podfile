platform :ios, '11.0'

source 'https://cdn.cocoapods.org/'

target 'Demo' do
  pod 'Forma', :path => './'
  pod 'Reveal-SDK', :configurations => ['Debug']
end

target 'Forma' do
  podspec :name => 'Forma'
end
