platform :ios, '11.0'

source 'https://github.com/CocoaPods/Specs.git'

target 'Demo' do
  pod 'Forma', :path => './'
  pod 'Reveal-SDK', :configurations => ['Debug']
end

target 'Forma' do
  podspec :name => 'Forma'
end
